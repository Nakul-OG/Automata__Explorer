# Automata__Explorer
Automata Explorer is a browser-based tool that converts regular expressions into NFA and DFA, generates accepted strings, and checks equivalence between expressions. It provides an interactive way to learn and visualize concepts from Theory of Computation using pure JavaScript.
